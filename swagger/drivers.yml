parameters:
  - name: format
    in: query
    type: string
    enum: ['json', 'xml']
    required: false
    default: json
  - name: driver_id
    in: query
    type: string
    required: false
    default: null
responses:
  200:
    description: JSON or XML report about racers

